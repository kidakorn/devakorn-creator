# แผนการพัฒนาและแก้ไขปัญหาโปรเจกต์ (Roadmap)

## 1. ปรับปรุงความเสถียรของหน้า Image Studio
- **ตรวจสอบการหักเหรียญ:** ทดสอบการตัดเหรียญในโหมด Standard Render (29 Coins) และ Studio Premium (49 Coins) ให้ถูกต้องตรงกับฐานข้อมูล (Neon DB)
- **จัดการ Error ฝั่ง UI:** ปรับข้อความแจ้งเตือน Error หน้าบ้านให้เข้าใจง่ายในกรณีที่ AI ทำงานหนัก (เช่น Server Overloaded) แทนการแสดงผลแบบ JSON Error

## 2. ติดตั้งระบบ 2 ภาษา (TH/EN) ทั่วทั้งเว็บ
- **สร้าง Dictionary:** รวบรวมคำศัพท์หลักๆ ของระบบ เช่น ในส่วน Dashboard, Sidebar และปุ่มคำสั่งต่างๆ
- **ระบบจำค่าภาษา:** เขียน Logic ให้ระบบจดจำภาษาที่ผู้ใช้เลือกผ่าน `localStorage` เพื่อไม่ให้ภาษาเปลี่ยนกลับเวลา Refresh หน้าเว็บ

## 3. จัดการหน้า Wallet & History (ประวัติการใช้งาน)
- **ตารางประวัติธุรกรรม:** ดึงข้อมูลประวัติจากตาราง Transaction มาแสดงผลแยกให้ชัดเจน (เช่น สร้างรูป, วิดีโอ หรือพรอมต์)
- **สถานะการทำงาน:** แสดงสถานะของการเจนให้ชัดเจน (`COMPLETED` หรือ `FAILED`) เพื่อให้ผู้ใช้มั่นใจว่าถ้างานรันไม่สำเร็จ เหรียญในกระเป๋าจะไม่ถูกหัก

## 4. ระบบชำระเงิน (Payment System)
- **Thunder API Integration:** เขียน API สำหรับส่งรูปสลิปไปตรวจสอบยอดโอน และเติมเหรียญให้ผู้ใช้อัตโนมัติเมื่อตรวจสอบผ่าน
- **QR Code Modal:** สร้างหน้าต่างป๊อปอัปแสดง QR Code (พร้อมเพย์) และเพิ่มช่องสำหรับอัปโหลดสลิป ในส่วนของหน้า Wallet

## 5. เตรียมความพร้อมของ VPS และฐานข้อมูลจริง (Production Prep)
- **Sync ฐานข้อมูล:** รัน `npx prisma db push` บน Database ของเซิร์ฟเวอร์ Production เพื่ออัปเดต Schema ให้ตรงกับของฝั่ง Dev
- **Environment Variables:** ตรวจสอบและกรอก API Keys ทั้งหมดบน VPS ให้ครบถ้วน (Google Cloud Agent Platform, Cloudinary, Stripe, Thunder API)
